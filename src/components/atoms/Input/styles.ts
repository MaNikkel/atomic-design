import styled from "styled-components"

export const Container = styled.input<{ variant?: 'primary' | 'light' }>`
  padding: 8px 16px;
  color: #000;
  background: #fff;
  border: 1px solid #000;
  border-radius: 5px;
  
`